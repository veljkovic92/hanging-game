const input = document.querySelector(".input")
const input2 = document.querySelector(".input2")
const btn = document.querySelector(".btn")
const btn2 = document.querySelector(".btn2")
const letters = document.querySelector(".letters")
const lives = document.querySelector(".lives")
const img = document.querySelector(".img")
let startGame = false;
let spanEl = document.getElementsByTagName("span")

btn.addEventListener("click", function(){
  const word = input.value
  img.setAttribute("src", "images/pic1.jpg")
  btn.textContent = "Try"
  const wordChar = word.split("")

if (startGame === false) {

  for (let i = 0; i < word.length; i ++) {

    let newEl = document.createElement("span")
    newEl.textContent = "?"
    letters.appendChild(newEl)
    let newEl2 = document.createElement("span")
    newEl2.textContent = wordChar
    letters.appendChild(newEl2)
    newEl2.classList.add("hidden")

  }
  startGame = true
  btn.classList.remove("btn")
  input.classList.remove("input")
}

btn2.addEventListener("click", function(){
  let word2 = input2.value

  if (word2.length > 1) {
    alert("Type only one character")
  } else {
    if (wordChar.indexOf(word2) >= 0) {
        wordTest(word2, wordChar)
    }
  }
})


})

function wordTest (word2, wordChar) {
let noOfTries = wordChar.length
if (noOfTries === 0) {
  gameOver()
  alert("game over")
} else {
  noOfTries--
  console.log(noOfTries)
  for(let i = 0; i < spanEl.length; i ++) {
    if (spanEl[i].innerText === wordChar[i]) {
      spanEl[i].innerText = wordChar[i]
    }

  }
}


}
